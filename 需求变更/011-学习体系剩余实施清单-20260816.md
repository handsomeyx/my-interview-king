# 学习体系改造 · 剩余实施清单

> 执行人须知：本文档列出当前改造方案中尚未完成的实施项。每项任务都标注了范围、具体操作、示例和验收标准。遇到不确定的地方，参考已完成文章的风格和语气。

---

## 任务 1：在核心文章正文推导处加入「读者思考」微提示

### 目标

在长文章的关键推导步骤中，用一个简短的引用块打断阅读节奏，引导读者暂停几秒自己思考。**不同于文章末尾的「复述自测」——这个是在阅读过程中间的"刹车"。**

### 格式

```
> 🤔 停下来想想：[具体的引导性问题]
```

关键：问题不需要很深奥，但必须让读者"停下来想"。不超过 20 个字最好。

### 实施范围

挑以下 8 篇文章，每篇加 2-3 个，选择**核心推导步骤**处（不要每段都加）。

#### 1.1 `docs/java/concurrent/volatile-jmm.md`

| 插入位置 | 内容 |
|---------|------|
| Q2 讲完"as-if-serial"后（第 41 行附近） | `> 🤔 停下来想想：单线程下重排序永远观察不到——这是为什么？` |
| Q3 讲完"volatile 写的 StoreLoad 最贵"后（第 60 行附近） | `> 🤔 停下来想想：为什么 volatile 写比读慢这么多？` |
| Q5 讲完"DCL 为什么要 volatile"后（第 101 行附近） | `> 🤔 停下来想想：如果没有 volatile，2 和 3 重排序后会发生什么？` |

#### 1.2 `docs/java/mysql/transaction-mvcc.md`

| 插入位置 | 内容 |
|---------|------|
| Q2 讲完四种隔离级别后（约第 34 行） | `> 🤔 停下来想想：不可重复读和幻读的本质区别是什么？` |
| Q3 讲完 MVCC 三件套后（约第 55 行） | `> 🤔 停下来想想：为什么快照读能不加锁？` |
| Q4 讲完 RC 和 RR 的区别后 | `> 🤔 停下来想想：ReadView 在 RC 和 RR 下生成时机不同，这带来了什么行为差异？` |

#### 1.3 `docs/java/spring/ioc-aop.md`

| 插入位置 | 内容 |
|---------|------|
| Q3 讲完 Bean 生命周期 7 步后（约第 50 行） | `> 🤔 停下来想想：AOP 代理在第 6 步才生成，那构造方法里调 @Transactional 为什么会失效？` |
| Q4 讲完 JDK vs CGLIB 对比后（约第 60 行） | `> 🤔 停下来想想：SpringBoot 2.x 为什么默认改用 CGLIB？` |

#### 1.4 `docs/distributed/basics.md`

| 插入位置 | 内容 |
|---------|------|
| Q1 讲完"CAP 不是任何时候三选二"后（约第 28 行） | `> 🤔 停下来想想：没有网络分区时，CAP 三选二还成立吗？` |
| Q5 讲完"多数派 Quorum"后（约第 73 行） | `> 🤔 停下来想想：为什么多数派能保证一致性？任意两个多数派必有交集——这是为什么？` |

#### 1.5 `docs/algorithm/00-algorithm-frameworks/sliding-window/index.md`

| 插入位置 | 内容 |
|---------|------|
| 框架模板前（约第 33 行） | `> 🤔 停下来想想：为什么滑动窗口能把 O(n²) 降到 O(n)？` |
| 四种类型讲完后 | `> 🤔 停下来想想：什么时候该扩大窗口、什么时候该缩小？判断依据是什么？` |

#### 1.6 `docs/java/jvm/garbage-collection.md`

在 GC 种类对比、可达性分析讲解、GC 调优思路处各挑一处。

#### 1.7 `docs/java/redis/persistence-cache-problems.md`

在 AOF 重写、缓存穿透/击穿/雪崩讲解处各挑一处。

#### 1.8 `docs/java/concurrent/lock.md`

在 synchronized 锁升级过程、AQS 核心流程讲解处各挑一处。

### 验收标准

- 每篇文章出现 2-3 个 `> 🤔 停下来想想：` 引用块
- 问题紧贴上下文推导，不是泛泛而谈
- 不影响原文流畅度

---

## 任务 2：为练习题文章追加「复盘三步」

### 目标

练习题文章目前只有题目和解法，缺少"做完之后该怎么复盘"的引导。追加复盘三步，打通"做题→复盘→举一反三"的闭环。

### 格式

在文章末尾（代码示例和总结之后）追加以下内容：

```markdown
## 做完之后试试

1. **盖住答案重新做一遍**：不看本文的解法提示，你能在 20 分钟内独立写出来吗？
2. **用一句话讲清楚**：如果让你给面试官讲这道题的思路，你会怎么说？（不能直接念代码）
3. **举一反三**：这道题的思路可以用来解决哪些其他题？（可以回头看看对应框架章节的内容）
```

### 实施范围

以下文件全部追加：

| 文件路径 |
|---------|
| `docs/algorithm/06-practice/sliding-window-practice.md` |
| `docs/algorithm/06-practice/dynamic-programming-practice-i.md` |
| `docs/algorithm/06-practice/dynamic-programming-practice-ii.md` |
| `docs/algorithm/06-practice/dfs-bfs-practice-i.md` |
| `docs/algorithm/06-practice/dfs-bfs-practice-ii.md` |
| `docs/algorithm/06-practice/binary-tree-practice-i.md` |
| `docs/algorithm/06-practice/binary-tree-practice-ii.md` |
| `docs/algorithm/06-practice/binary-search-practice.md` |
| `docs/algorithm/06-practice/backtrack-practice-i.md` |

### 验收标准

- 每篇练习题末尾有「复盘三步」段落
- 格式统一，文字一致

---

## 任务 3：补齐 `thinking-links.json` 跨域关联配置

### 目标

当前 `thinking-links.json` 只有 9 个页面的配置。需要补充以下高频页面的跨域关联和读者思考问题。

### 操作

直接编辑 `docs/.vitepress/theme/thinking-links.json`，在现有对象中追加以下条目：

#### 3.1 Redis 持久化

```json
"java/redis/persistence-cache-problems": {
  "related": [
    { "title": "MySQL 事务与 MVCC", "url": "/java/mysql/transaction-mvcc", "reason": "AOF 和 redo log 都是写前日志（WAL）思想，保证提交后数据不丢" },
    { "title": "Kafka", "url": "/java/kafka/index", "reason": "Kafka 的 commit log 和 Redis AOF 本质上都是追加写日志，追加写性能远高于随机写" },
    { "title": "缓存问题", "url": "/java/redis/persistence-cache-problems#缓存穿透击穿雪崩", "reason": "缓存穿透/击穿/雪崩和分布式系统的降级、限流是同一类问题" }
  ],
  "think": [
    "Redis AOF 的重写和 MySQL 的 purge binlog 解决的是同一类问题吗？",
    "缓存穿透、击穿、雪崩的核心区别是什么？各自的解法思路有什么本质不同？"
  ]
}
```

#### 3.2 JVM GC

```json
"java/jvm/garbage-collection": {
  "related": [
    { "title": "内存区域", "url": "/java/jvm/memory-area", "reason": "GC 发生在堆和方法区（元空间），理解内存区域是理解 GC 的基础" },
    { "title": "Redis 数据结构", "url": "/java/redis/data-structures", "reason": "JVM 对象回收和 Redis 过期删除都是「空间回收」问题，策略不同" },
    { "title": "MySQL 索引", "url": "/java/mysql/indexing", "reason": "GC 的标记-清除和 B+ 树的页分裂都涉及空间碎片的处理" }
  ],
  "think": [
    "为什么 G1 用 SATB 而 CMS 用增量更新？这对实现复杂度和停顿时间各有什么影响？",
    "ZGC 和 Shenandoah 都追求亚毫秒停顿，它们的核心手段是什么？和 G1 有什么本质区别？"
  ]
}
```

#### 3.3 Spring 事务

```json
"java/spring/transaction": {
  "related": [
    { "title": "AOP 原理", "url": "/java/spring/ioc-aop", "reason": "@Transactional 本质是 AOP 环绕通知，理解 AOP 才能理解事务失效场景" },
    { "title": "MySQL 事务与 MVCC", "url": "/java/mysql/transaction-mvcc", "reason": "Spring 事务的隔离级别最终设置到数据库，传播行为是对数据库事务的封装" },
    { "title": "分布式基础", "url": "/distributed/basics", "reason": "本地事务是单机 ACID，分布式事务是 CAP 取舍下的妥协" }
  ],
  "think": [
    "为什么 @Transactional 自调用会失效？除了自调用还有哪些失效场景？",
    "Spring 的传播行为中，PROPAGATION_REQUIRED 和 PROPAGATION_REQUIRES_NEW 的核心区别是什么？"
  ]
}
```

#### 3.4 MySQL 索引

```json
"java/mysql/indexing": {
  "related": [
    { "title": "Redis 数据结构", "url": "/java/redis/data-structures", "reason": "B+ 树、跳表、哈希索引都是在「查找效率 vs 更新效率」间找工程平衡点" },
    { "title": "树结构", "url": "/algorithm/02-data-structures/tree/", "reason": "B+ 树是多路平衡查找树，和算法中的红黑树、AVL 树同属树结构家族" },
    { "title": "一致性哈希", "url": "/algorithm/04-system-algorithms/consistent-hashing", "reason": "哈希索引和一致性哈希共享哈希思想，解决不同场景的查找问题" }
  ],
  "think": [
    "为什么 InnoDB 主键建议用自增整型？如果用 UUID 作为主键会有什么性能问题？",
    "覆盖索引和索引条件下推的区别是什么？它们对查询性能的影响分别是什么？"
  ]
}
```

#### 3.5 分布式场景 - 网关

```json
"distributed/scenarios/gateway": {
  "related": [
    { "title": "一致性哈希", "url": "/algorithm/04-system-algorithms/consistent-hashing", "reason": "网关的负载均衡策略（轮询、权重、哈希）和一致性哈希共享路由思想" },
    { "title": "限流算法", "url": "/algorithm/04-system-algorithms/rate-limiting", "reason": "网关是限流的最佳实施位置，令牌桶/漏桶算法在网关层直接应用" },
    { "title": "服务治理", "url": "/distributed/scenarios/service", "reason": "网关负责接入路由，服务治理负责内部服务发现和调用" }
  ],
  "think": [
    "网关层做限流、服务层做限流、数据库层做限流，三者的职责边界是什么？",
    "为什么很多公司同时使用 Nginx 网关和 Spring Cloud Gateway？它们的定位有什么不同？"
  ]
}
```

#### 3.6 系统算法 - 缓存

```json
"algorithm/04-system-algorithms/caching": {
  "related": [
    { "title": "Redis 数据结构", "url": "/java/redis/data-structures", "reason": "LRU/LFU 是 Redis 内存淘汰策略的算法基础" },
    { "title": "Redis 缓存问题", "url": "/java/redis/persistence-cache-problems", "reason": "缓存穿透/击穿/雪崩是缓存设计必须考虑的异常场景" },
    { "title": "JVM 内存区域", "url": "/java/jvm/memory-area", "reason": "JVM 的本地缓存、进程间的分布式缓存、CDN 缓存，构成多层缓存体系" }
  ],
  "think": [
    "LRU、LFU、LRU-K 三者的核心区别是什么？各自适用什么场景？",
    "为什么工业界很少用纯正 LRU？它的主要缺陷是什么？"
  ]
}
```

#### 3.7 系统算法 - 限流

```json
"algorithm/04-system-algorithms/rate-limiting": {
  "related": [
    { "title": "分布式基础", "url": "/distributed/basics", "reason": "限流在分布式场景下需要考虑一致性（各节点限流计数同步）" },
    { "title": "网关场景", "url": "/distributed/scenarios/gateway", "reason": "网关是限流的最佳实施位置之一" },
    { "title": "Kafka", "url": "/java/kafka/index", "reason": "消息队列本身是削峰限流的一种手段" }
  ],
  "think": [
    "令牌桶和漏桶的核心区别是什么？一个允许突发流量一个不允许——这是为什么？",
    "滑动窗口、固定窗口、滑动日志三者的精度和实现复杂度如何权衡？"
  ]
}
```

#### 3.8 算法框架 - 二分搜索

```json
"algorithm/00-algorithm-frameworks/binary-search": {
  "related": [
    { "title": "MySQL 索引", "url": "/java/mysql/indexing", "reason": "B+ 树索引的查找本质上是二分搜索的多路版本" },
    { "title": "Redis 数据结构", "url": "/java/redis/data-structures", "reason": "跳表、有序数组的查找都用了二分思想" },
    { "title": "复杂度分析", "url": "/algorithm/01-methodology/complexity-guide", "reason": "二分搜索是 O(log n) 的典型代表" }
  ],
  "think": [
    "二分搜索的时间复杂度是 O(log n)，这个 log 的底数是多少？为什么通常省略不写？",
    "二分搜索的变种（找左边界、找右边界）和标准二分的核心区别是什么？"
  ]
}
```

### 验收标准

- `thinking-links.json` 中以上 8 个页面条目完整加入
- JSON 格式正确，逗号/括号无误
- 每个条目有 2-3 个 `related` 关联和 2 个 `think` 问题

---

## 任务 4：首页学习路径卡片追加引导文案

### 目标

在 `LearningPath.vue` 的学习路径底部追加一行引导文字，鼓励用户标记掌握后回到文章做复述自测。

### 操作

编辑 `docs/.vitepress/theme/LearningPath.vue`，找到第 52 行：

```html
<p class="lp-hint">点圆点切换掌握状态（待学 → 学习中 → 已掌握），点文字跳转章节。状态本地保存。</p>
```

在它后面追加一行：

```html
<p class="lp-hint">💡 建议每学完一个节点，回到对应文章末尾做一下「复述自测」——能讲出来才算真正掌握。</p>
```

### 验收标准

- 首页学习路径卡片底部出现引导文字
- 不影响现有样式

---

## 任务 5：实现间隔重复提醒（纯前端版本）

### 目标

当用户标记某篇文章为「已掌握」后，系统在 1/3/7 天后自动提醒回来重新做复述自测。

### 技术方案

#### 5.1 数据结构

在 `study-storage.ts` 中追加一个新的 localStorage key：

```typescript
const KEYS = {
  // ...现有 KEYS
  reviewReminders: 'ik:review-reminders'  // 新增
}

type ReviewReminder = {
  path: string
  title: string
  masteredAt: number
  remindAt: number
  stage: number  // 0=1天, 1=3天, 2=7天, 3=已完成
}
```

#### 5.2 新增函数

在 `study-storage.ts` 中追加：

```typescript
const INTERVALS = [1, 3, 7]  // 天

export function scheduleReview(path: string, title: string) {
  const all = getReviewReminders()
  const now = Date.now()
  const days = INTERVALS[0]
  const remindAt = now + days * 24 * 60 * 60 * 1000
  all[path] = { path, title, masteredAt: now, remindAt, stage: 0 }
  write(KEYS.reviewReminders, all)
}

export function getReviewReminders(): Record<string, ReviewReminder> {
  return read(KEYS.reviewReminders, {})
}

export function getDueReviews(): ReviewReminder[] {
  const all = getReviewReminders()
  const now = Date.now()
  return Object.values(all).filter(r => r.remindAt <= now && r.stage < INTERVALS.length)
}

export function advanceReview(path: string) {
  const all = getReviewReminders()
  const r = all[path]
  if (!r) return
  if (r.stage >= INTERVALS.length - 1) {
    delete all[path]
  } else {
    const nextStage = r.stage + 1
    const days = INTERVALS[nextStage]
    r.stage = nextStage
    r.remindAt = Date.now() + days * 24 * 60 * 60 * 1000
    all[path] = r
  }
  write(KEYS.reviewReminders, all)
}

export function cancelReview(path: string) {
  const all = getReviewReminders()
  delete all[path]
  write(KEYS.reviewReminders, all)
}
```

#### 5.3 触发时机

在 `LearningPath.vue` 的 `cycle` 函数中，当状态切换到 `mastered` 时调用 `scheduleReview(path, title)`；当从 `mastered` 切回其他状态时调用 `cancelReview(path)`。

#### 5.4 展示提醒

在 `MyProgressPanel.vue` 中追加"复习提醒"板块：

```vue
<div v-if="dueReviews.length" class="mp-section">
  <h3>复习提醒</h3>
  <div v-for="r in dueReviews" :key="r.path" class="mp-item review-item">
    <a :href="r.path" class="review-link">{{ r.title }}</a>
    <button class="review-done" @click="markReviewed(r.path)">已复习</button>
    <button class="review-skip" @click="skipReview(r.path)">跳过</button>
  </div>
</div>
```

并在 script 中计算 `dueReviews`、实现 `markReviewed`（调用 `advanceReview`）和 `skipReview`（调用 `cancelReview`）。

#### 5.5 扩展 `ContinueReading.vue`

在"继续学习"卡片中，如果某篇文章有到期的复习提醒，在卡片旁加一个小图标提示：

```
⏰ 该复习了
```

### 验收标准

- 用户标记掌握后，localStorage 中出现对应的复习提醒条目
- 到期后「我的学习」页面显示复习提醒列表
- 点击"已复习"推进到下一间隔（1→3→7 天）
- 跳过则取消该文章的复习提醒

---

## 任务执行顺序建议

| 顺序 | 任务 | 预计耗时 | 前置依赖 |
|------|------|---------|---------|
| 1 | 任务 2：练习题复盘三步 | 30 分钟 | 无 |
| 2 | 任务 1：文中思考微提示 | 1-2 小时 | 无 |
| 3 | 任务 4：学习路径引导文案 | 10 分钟 | 无 |
| 4 | 任务 3：补齐 thinking-links.json | 30 分钟 | 无 |
| 5 | 任务 5：间隔重复提醒 | 2-3 小时 | 有（需理解 study-storage.ts 和 LearningPath.vue） |

任务 1-4 互相独立，可以并行。任务 5 需要理解现有组件逻辑，放最后做。

---

## 验收总清单

完成全部任务后，逐项验证：

- [ ] 8 篇核心文章各有 2-3 个 `> 🤔 停下来想想：` 引用块
- [ ] 9 篇练习题末尾有「复盘三步」段落
- [ ] `thinking-links.json` 包含 17 个页面的跨域关联配置
- [ ] 首页学习路径卡片底部有复述自测引导文案
- [ ] `study-storage.ts` 新增间隔重复相关函数
- [ ] `LearningPath.vue` 掌握状态切换时触发/取消复习提醒
- [ ] `MyProgressPanel.vue` 显示到期复习提醒
- [ ] `ContinueReading.vue` 显示复习提醒图标
- [ ] 刷新页面后功能正常，无控制台报错
